"""Integrações com serviços de terceiros em nome de UM membro (OAuth).

Diferente de `channels/` (por onde a família FALA) e de `tools/` (o que o LLM
pode fazer): aqui mora a credencial de cada pessoa em um serviço externo.

Primeira integração: Google Calendar (piloto — ver docs/adr/010).
"""
